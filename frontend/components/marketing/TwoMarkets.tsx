export function TwoMarkets() {
  return (
    <section className="border-t border-border bg-surface/40">
      <div className="mx-auto max-w-3xl px-6 py-16 text-center sm:py-20">
        <h2 className="mb-4 text-2xl font-bold text-text sm:text-3xl">
          Two markets, both first-class
        </h2>
        <p className="text-sm leading-relaxed text-muted sm:text-base">
          Nigerian and US stocks are never blended into one list. Each gets
          its own full view, its own sector categories, its own bucket
          rankings, a real differentiator against research tools that only
          ever cover one market.
        </p>
      </div>
    </section>
  );
}
